<Page x:Class="biblioteka.VidachaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:biblioteka"
      xmlns:System="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="VidachaPage" Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выдача книг" Style="{StaticResource TitleText}" />

        <StackPanel Grid.Row="1" Orientation="Vertical" Width="400" HorizontalAlignment="Center">

            <Label Content="Выберите книгу:" />
            <ComboBox x:Name="BookComboBox" DisplayMemberPath="Название" SelectedValuePath="ID_Книги"/>

            <Label Content="Выберите студента:" />
            <ComboBox x:Name="StudentComboBox" DisplayMemberPath="ФИО" SelectedValuePath="ID_Студента"/>

            <Label Content="Срок возврата:" />
            <DatePicker x:Name="DataPicker" SelectedDate="{x:Static System:DateTime.Now}" />

            <Button Content="Выдать книгу" Click="Book_Click" />
            <Button Content="Назад" Margin="5" Click="GoBack_Click"/>
        </StackPanel>
    </Grid>
</Page>
